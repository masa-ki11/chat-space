.side-ber
  .side-ber__user-name
    .side-ber__top-items
      .side-ber__top-items__name
        = current_user.name
      .side-ber__top-items__icons
        = link_to "#", class: "icons__btn" do
          = icon('far', 'edit')
        = link_to edit_user_path(current_user), class: "icons__btn" do
          = icon('fas', 'cog')  
  .side-ber__group-list
    .group-name
      title
      .new-message
        message
    .group-name
      title
      .new-message
        message
    .group-name
      title
      .new-message
        message
    .group-name
      title
      .new-message
        message
